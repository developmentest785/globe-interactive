# Redirects for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Build settings
[build]
  publish = "dist"
  command = "pnpm run build"

# Development settings
[dev]
  command = "pnpm run dev"
  port = 8888
  targetPort = 5173

